# ArcaneAudio Python dependencies
# Setup (Windows):  py -3.13 -m venv .venv && .venv\Scripts\python -m pip install -r requirements.txt
# Setup (Linux):    uv venv --python 3.13 --seed && uv pip install -r requirements.txt   # uv reads .python-version (3.13)
#
# Runtime: PySide6 (Qt for Python; GUI + QtMultimedia playback)
#          pyqtgraph + numpy  (audio visualizer)
#          yt-dlp             (YouTube → MP3 "Downlord" feature; needs ffmpeg, Windows-bundled)
#          mutagen            (MP3 tag reading)
#          Pillow             (image handling)
# Build:   pyinstaller (only needed to produce the standalone app/installer)

PySide6==6.11.1     # Qt for Python — pinned for a reproducible, known-good binding
shiboken6==6.11.1   # PySide6's binding layer; must match PySide6 exactly
pyqtgraph
numpy
yt-dlp
mutagen
Pillow
pyinstaller
